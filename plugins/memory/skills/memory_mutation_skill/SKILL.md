# Memory Mutation Skill

Use `memory_add` only when the user explicitly asks to save or remember a
personal fact or preference for future conversations. It is not file storage
and does not satisfy requests to save documents, reports, artifacts, or
generated output.

Use `memory_delete` only with an exact memory ID. Retrieve memory first when the
ID is unknown.
